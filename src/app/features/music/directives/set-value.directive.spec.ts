import { SetValueDirective } from './set-value.directive';

describe('SetValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SetValueDirective();
    expect(directive).toBeTruthy();
  });
});
